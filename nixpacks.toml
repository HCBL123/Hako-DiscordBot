# nixpacks.toml

# 1. This ensures both languages are installed
providers = ["node", "python"]

# 2. Explicitly run install commands for both
[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

# 3. Start the bot
[start]
cmd = "node src/bot.js"